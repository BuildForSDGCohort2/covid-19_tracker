import React from "react";

const Header = (props) => {
    return (
        <header>
            <div className="wrap">
                
            </div>
        </header>
    );
};

export default Header;